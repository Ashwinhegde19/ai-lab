# Python dependencies for the Personal AI Lab
# Install: pip install -r requirements.txt
# Tested on Linux + Python 3.11

# ---- Core LLM stack ----
transformers>=4.44.0
accelerate>=0.34.0
peft>=0.12.0
trl>=0.9.0
datasets>=2.21.0
tokenizers>=0.19.0
sentencepiece>=0.2.0
safetensors>=0.4.0

# ---- Unsloth (fine-tuning acceleration) ----
unsloth>=2024.8
# Note: Unsloth requires CUDA. On Mac/CPU, comment out and use plain PEFT.

# ---- vLLM (serving) ----
vllm>=0.5.4
# Note: vLLM requires CUDA + Linux. Skip on Mac.

# ---- Quantization ----
autoawq>=0.2.6
auto-gptq>=0.7.1
bitsandbytes>=0.43.3

# ---- Structured output ----
outlines>=0.0.41
xgrammar>=0.1.0

# ---- RAG ----
chromadb>=0.5.5
FlagEmbedding>=1.2.10  # BGE-M3 + bge-reranker

# ---- Guardrails ----
# Llama Guard 3 and Prompt Guard are loaded via transformers directly
# No extra package needed.

# ---- Evaluation ----
scikit-learn>=1.5.1  # for cohen_kappa_score, precision/recall/F1
lm-eval>=0.4.4       # for public benchmarks (HarmBench, TruthfulQA, BBQ)

# ---- Observability ----
langfuse>=2.45.0     # client SDK
# Langfuse self-hosted server runs via docker-compose — see station-12-observability/

# ---- Backend / API ----
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.8.0

# ---- Data tooling ----
pandas>=2.2.0
numpy>=1.26.0
matplotlib>=3.9.0
jupyterlab>=4.2.0
ipykernel>=6.29.0

# ---- Modal (serverless GPU) ----
modal>=0.64.0

# ---- Utils ----
python-dotenv>=1.0.1
tqdm>=4.66.0
rich>=13.8.0
